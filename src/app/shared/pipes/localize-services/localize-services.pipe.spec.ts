import { LocalizeServicesPipe } from './localize-services.pipe';

describe('LocalizeServicesPipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizeServicesPipe();
    expect(pipe).toBeTruthy();
  });
});
